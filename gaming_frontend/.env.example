# Example environment configuration for gaming_frontend
# Copy this file to `.env` and fill in the values.

# Backend base URL for API calls (e.g., http://localhost:3001)
BACKEND_BASE_URL=http://localhost:3001

# Public site URL used for redirects (if integrating OAuth flows)
SITE_URL=http://localhost:8080
